# URL del API FastAPI (requerido para secretaría y médico con backend real)
VITE_API_BASE_URL=http://127.0.0.1:8000

# false = datos desde FastAPI; true = JSON demo local
VITE_USE_CLINICAL_MOCK=false

# Omite login real: entra con el correo del formulario (solo desarrollo)
# VITE_SKIP_AUTH=true
